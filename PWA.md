# PWA 我的旅行 — 專案規格文件

> 目的：建立一個私人使用的旅行規劃 PWA，方便在手機與電腦上規劃、瀏覽與編輯旅程。  
> 使用情境：個人 / 家庭旅行規劃，不上架、不收費、不依賴付費雲端服務。  
> 開發習慣：使用 VS Code 編輯，後續可交由 Codex 協助逐步開發。

---

## 1. 專案核心目標

這個 PWA 的目標是做成一個簡單、直覺、私人使用的旅行規劃工具。

主要特點：

- 不需要登入帳號
- 優先支援手機操作
- 可以離線使用
- 所有資料先存在本機
- 介面簡單，大按鈕，方便旅行時快速查看
- 未來若需要，可再加入 iCloud 備份 / 匯入匯出方案
- 目前不考慮付費資料庫或付費主機服務

---

## 2. 技術方向

### 2.1 建議技術

- Frontend：HTML / CSS / JavaScript 或 React
- App 型態：PWA
- 編輯器：VS Code
- AI 開發協助：Codex
- 本機資料儲存：IndexedDB
- 地圖功能：可考慮 Leaflet
- 雲端資料：暫不使用 Firestore，避免產生費用

### 2.2 PWA 優點

- 可以安裝到手機桌面
- 可以在 iPhone / Android / 電腦瀏覽器使用
- 不需要上架 App Store
- 私人使用成本低
- 維護比原生 App 簡單

### 2.3 未來轉 iPhone App

目前先做 PWA。  
未來若真的需要轉成 iPhone App，可以再評估：

- Capacitor
- TWA 類似方案
- Flutter 重新開發
- 原生 Swift App

但目前不建議一開始就做原生 App，因為成本與維護複雜度較高。

---

## 3. 免費使用原則

本專案希望「開發、使用、維護」都盡量不產生額外費用。

### 3.1 免費方案

優先採用：

- 本機 IndexedDB 儲存資料
- PWA 離線快取
- 使用者手動匯出 / 匯入 JSON 備份
- 使用 GitHub / 本機資料夾管理程式碼
- 不使用需要長期付費的後端服務

### 3.2 iCloud 備份構想

因為 PWA 無法直接控制 iCloud Drive，所以可採取間接方案：

- 在 App 內提供「匯出 JSON」功能
- 使用者手動把 JSON 存到 iCloud Drive
- 換手機或重裝瀏覽器後，再用「匯入 JSON」恢復資料

這樣可以做到：

- 不需要伺服器
- 不需要 Firestore
- 不需要帳號系統
- 不產生雲端資料庫費用

### 3.3 IndexedDB 注意事項

IndexedDB 資料存在瀏覽器本機。

優點：

- 免費
- 容量比 localStorage 大
- 適合存旅行資料、景點、備註、每日行程

限制：

- 如果使用者清除瀏覽器資料，資料可能會消失
- 如果手機重置或瀏覽器重裝，資料可能會消失
- 所以必須設計「匯出 / 匯入備份」功能

---

## 4. 介面設計原則

使用者偏好的 UI 方向：

- 介面簡單
- 每頁盡量一個畫面完成，不要太多捲動
- 大按鈕
- 顏色明顯
- 可使用 3D 風格按鈕
- 操作流程要直覺
- 手機上容易點擊

---

## 5. 整體頁面樹狀結構

```text
PWA 我的旅行
│
├── 首頁
│   ├── 開啟新旅程
│   └── 瀏覽旅程
│
├── 開啟新旅程頁面
│   ├── 旅程名稱
│   ├── 國家
│   ├── 地區
│   ├── 出發時間（日曆）
│   ├── 回程時間（日曆）
│   └── 確定
│
├── 瀏覽旅程頁面
│   ├── 旅程列表
│   ├── 點擊旅程
│   └── 進入旅程主頁
│
└── 旅程主頁
    ├── 交通
    ├── 美食
    ├── 待辦
    └── 行程
        │
        ├── 行程日期列表頁
        │   ├── Day 1 按鈕
        │   ├── Day 2 按鈕
        │   ├── Day 3 按鈕
        │   └── ...
        │
        └── 當天行程編輯頁
            ├── 食
            │   ├── 早餐文字框，預設：猶豫中
            │   ├── 午餐文字框，預設：猶豫中
            │   └── 晚餐文字框，預設：猶豫中
            │
            ├── 景
            │   ├── 景點名稱
            │   ├── 景點時間
            │   ├── 景點地址
            │   ├── 景點備註
            │   ├── 確認
            │   └── 確認後跳出視窗：
            │       ├── 是否編輯下一筆？
            │       └── 或回到當天行程頁面
            │
            ├── 行
            │   ├── 交通工具下拉選單
            │   │   ├── 開車
            │   │   ├── 地鐵
            │   │   ├── 火車
            │   │   ├── 巴士
            │   │   ├── 走路
            │   │   └── 飛機
            │   └── 交通備註
            │
            └── 備註事項
                └── 文字編輯框
```

---

## 6. 首頁需求

首頁只放兩個主要按鈕：

1. 開啟新旅程
2. 瀏覽旅程

### 6.1 首頁 UI 建議

- 畫面中央顯示 App 名稱：我的旅行
- 下方兩個大型 3D 按鈕
- 手機直向排列
- 按鈕文字要大
- 不放太多說明文字

---

## 7. 開啟新旅程頁面

使用者點擊「開啟新旅程」後，進入新旅程建立頁。

欄位：

| 欄位 | 類型 | 說明 |
|---|---|---|
| 名稱 | 文字輸入 | 例如：義大利親子旅行 |
| 國家 | 文字輸入 | 例如：義大利 |
| 地區 | 文字輸入 | 例如：米蘭、威尼斯、佛羅倫斯 |
| 出發時間 | 日期選擇器 | 使用日曆選擇 |
| 回程時間 | 日期選擇器 | 使用日曆選擇 |
| 確定 | 按鈕 | 建立旅程 |

按下「確定」後：

- 儲存旅程資料
- 自動產生旅行日期範圍
- 進入旅程主頁

---

## 8. 瀏覽旅程頁面

顯示已建立的旅程列表。

每個旅程卡片可顯示：

- 旅程名稱
- 國家
- 地區
- 出發日期
- 回程日期

點擊某個旅程後，進入該旅程主頁。

---

## 9. 旅程主頁

旅程主頁有四個主要功能按鈕：

1. 交通
2. 美食
3. 待辦
4. 行程

目前已討論較完整的是「行程」功能。

---

## 10. 行程功能

### 10.1 行程日期列表頁

使用者點擊「行程」後，不是直接進入編輯頁，而是先顯示每一天的按鈕。

例如：

- Day 1：2026/06/25
- Day 2：2026/06/26
- Day 3：2026/06/27
- Day 4：2026/06/28

使用者點擊某一天後，進入「當天行程編輯頁」。

---

## 11. 當天行程編輯頁

當天行程分成四塊：

1. 食
2. 景
3. 行
4. 備註事項

---

## 12. 食：餐食規劃

「食」區塊包含三個文字編輯框：

| 項目 | 預設文字 |
|---|---|
| 早餐 | 猶豫中 |
| 午餐 | 猶豫中 |
| 晚餐 | 猶豫中 |

需求：

- 每個欄位都可以自由輸入
- 預設值都是「猶豫中」
- 可填餐廳名稱、地址、訂位時間、備註

資料範例：

```json
{
  "breakfast": "猶豫中",
  "lunch": "米蘭中央車站附近餐廳",
  "dinner": "已訂位 19:00，室內靠窗四人桌"
}
```

---

## 13. 景：景點規劃

「景」區塊用來編輯當天景點。

每筆景點可包含：

- 景點名稱
- 預計時間
- 地址
- 備註
- 是否已確認

### 13.1 景點編輯流程

使用者填完一筆景點後，按下「確認」。

按下確認後，跳出視窗詢問：

```text
是否要繼續編輯下一筆景點？

[編輯下一筆] [回到當天行程]
```

如果選擇「編輯下一筆」：

- 清空景點輸入欄位
- 保留在景點編輯區
- 繼續新增下一筆景點

如果選擇「回到當天行程」：

- 關閉景點編輯流程
- 回到當天行程總覽

---

## 14. 行：交通規劃

「行」區塊包含交通工具下拉式選單。

選項：

- 開車
- 地鐵
- 火車
- 巴士
- 走路
- 飛機

可再加入一個交通備註文字框，例如：

- 車票時間
- 班次
- 租車資訊
- 停車資訊
- 車站名稱
- 轉乘提醒

資料範例：

```json
{
  "transportType": "火車",
  "transportNote": "09:15 從米蘭中央車站出發"
}
```

---

## 15. 備註事項

第四塊是「備註事項」。

用途：

- 當天提醒
- 飯店入住資訊
- 門票資訊
- 特殊注意事項
- 付款提醒
- 行李寄放
- 小孩用品
- 天氣提醒

建議使用多行文字框。

---

## 16. 資料結構草案

以下是目前建議的資料結構，後續可依實作調整。

```json
{
  "trips": [
    {
      "id": "trip_001",
      "name": "義大利親子旅行",
      "country": "義大利",
      "region": "米蘭、威尼斯、佛羅倫斯",
      "startDate": "2026-06-25",
      "endDate": "2026-07-04",
      "days": [
        {
          "id": "day_001",
          "date": "2026-06-25",
          "title": "Day 1",
          "meals": {
            "breakfast": "猶豫中",
            "lunch": "猶豫中",
            "dinner": "猶豫中"
          },
          "spots": [
            {
              "id": "spot_001",
              "name": "米蘭大教堂",
              "time": "10:00",
              "address": "P.za del Duomo, Milano",
              "note": "記得先買票",
              "confirmed": false
            }
          ],
          "transport": {
            "type": "地鐵",
            "note": "注意班次與出口"
          },
          "dailyNote": "今天先熟悉附近環境"
        }
      ],
      "todo": [],
      "food": [],
      "traffic": []
    }
  ]
}
```

---

## 17. IndexedDB 儲存建議

可以建立以下 object stores：

```text
travelDB
│
├── trips
├── days
├── spots
├── todos
├── foodPlaces
└── trafficItems
```

初期也可以簡化成只用一個 `trips` store，把整個旅程 JSON 存進去。

### 17.1 初期建議

第一版先簡單：

```text
object store: trips
key: trip.id
value: 整個 trip JSON
```

等功能變複雜後，再拆成多個 stores。

---

## 18. 匯出 / 匯入備份功能

因為資料存在 IndexedDB，必須做備份功能。

### 18.1 匯出

功能：

- 將所有旅程資料轉成 JSON
- 下載成 `.json` 檔
- 使用者可自行存到 iCloud Drive / Google Drive / 電腦

檔名建議：

```text
my-travel-backup-YYYY-MM-DD.json
```

### 18.2 匯入

功能：

- 使用者選擇 JSON 檔
- App 讀取資料
- 寫回 IndexedDB
- 顯示匯入成功

---

## 19. 目前先不做的功能

以下功能先不要放進第一版，避免專案太大：

- 使用者登入
- 雲端同步
- 多人共享編輯
- 付款功能
- 複雜地圖路線規劃
- 自動抓取訂房信
- 自動讀取 Gmail
- 原生 iOS App
- Firestore 後端

---

## 20. 第一版 MVP 建議

第一版只做最核心功能。

### 20.1 MVP 功能

1. 首頁
2. 建立新旅程
3. 瀏覽旅程
4. 旅程主頁四大按鈕
5. 行程日期列表
6. 當天行程編輯頁
7. 食 / 景 / 行 / 備註事項
8. IndexedDB 儲存
9. 匯出 JSON
10. 匯入 JSON

### 20.2 MVP 暫緩功能

- 地圖
- 景點照片
- 複雜交通時間計算
- 餐廳資料庫
- 多裝置自動同步

---

## 21. Codex 開發提示詞建議

把這份 `PWA.md` 放在專案根目錄後，可以在 Codex 裡使用以下方向開發。

### 21.1 初始提示詞

```text
請讀取 PWA.md，根據裡面的規格建立一個私人旅行規劃 PWA。

開發要求：
1. 優先完成 MVP。
2. 使用簡單清楚的 UI。
3. 頁面以手機優先設計。
4. 資料先存在 IndexedDB。
5. 先不要串接任何付費雲端服務。
6. 先完成：首頁、新旅程、瀏覽旅程、旅程主頁、行程日期列表、當天行程編輯頁。
7. 每個功能完成後，請說明修改了哪些檔案。
```

### 21.2 分階段開發提示詞

#### 第 1 階段：建立專案骨架

```text
請根據 PWA.md 建立專案骨架，包含首頁、路由架構、基本 CSS、PWA manifest、service worker。
先不要實作 IndexedDB，只要完成畫面切換。
```

#### 第 2 階段：建立新旅程流程

```text
請實作「開啟新旅程」頁面。
欄位包含：名稱、國家、地區、出發時間、回程時間、確定按鈕。
按下確定後，自動產生日期列表，並進入旅程主頁。
```

#### 第 3 階段：實作行程功能

```text
請實作「行程」功能。
點擊行程後，先顯示每一天的按鈕。
點擊某一天後，進入當天行程編輯頁。
當天行程分成四塊：食、景、行、備註事項。
```

#### 第 4 階段：加入 IndexedDB

```text
請加入 IndexedDB 儲存功能。
第一版只需要建立 trips store，把整個 trip JSON 存進去。
請確保重新整理頁面後資料仍然存在。
```

#### 第 5 階段：加入匯出 / 匯入

```text
請加入 JSON 匯出 / 匯入功能。
匯出時下載 my-travel-backup-YYYY-MM-DD.json。
匯入時讀取 JSON 並寫回 IndexedDB。
```

---

## 22. 開發優先順序

建議順序：

1. 畫面架構
2. 頁面切換
3. 建立旅程資料
4. 日期自動產生
5. 行程日期列表
6. 當天行程四大區塊
7. IndexedDB 儲存
8. 匯出 / 匯入
9. 美化 UI
10. 再討論交通 / 美食 / 待辦細節

---

## 23. 待討論事項

以下功能尚未完整規劃，後續再討論：

### 23.1 交通頁

目前只有旅程主頁上的「交通」按鈕，詳細頁面尚未規劃。

可能包含：

- 租車資訊
- 火車票
- 飛機資訊
- 停車資訊
- 交通備註

### 23.2 美食頁

目前只有旅程主頁上的「美食」按鈕，詳細頁面尚未規劃。

可能包含：

- 餐廳名稱
- 地址
- 訂位時間
- 是否已訂位
- 備註
- 想吃 / 已吃 / 放棄

### 23.3 待辦頁

目前只有旅程主頁上的「待辦」按鈕，詳細頁面尚未規劃。

可能包含：

- 護照
- 機票
- 住宿確認
- 租車確認
- 門票
- 行李
- 保險
- 網卡
- 信用卡

---

## 24. 目前確認的設計決策

- 這是私人旅行 PWA，不是公開服務
- 優先免費
- 優先本機儲存
- 不使用 Firestore 作為第一版資料庫
- 需要匯出 / 匯入備份
- 首頁只有「開啟新旅程」與「瀏覽旅程」
- 旅程主頁有四個大功能：交通、美食、待辦、行程
- 行程點開後先顯示每一天按鈕
- 點擊某一天後進入當天行程編輯
- 當天行程分成四塊：食、景、行、備註事項
- 食：早餐 / 午餐 / 晚餐，預設「猶豫中」
- 景：每次確認後詢問是否繼續編輯下一筆
- 行：交通工具下拉選單
- 備註事項：自由文字輸入

---

## 25. 給 Codex 的總結

請根據本文件開發一個手機優先、私人使用、免費維護的旅行規劃 PWA。

第一版不要過度設計，只完成 MVP：

- 建立旅程
- 瀏覽旅程
- 每日行程
- 食 / 景 / 行 / 備註事項
- IndexedDB 儲存
- JSON 匯出 / 匯入

後續再逐步擴充交通、美食、待辦與地圖功能。

