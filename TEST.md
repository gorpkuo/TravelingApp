# 手動測試清單（MVP）

## 1. 啟動方式

在 `c:\VSCode\TravelingApp` 執行（建議先用 `.bat`）：

```powershell
.\start.bat
```

或擇一：

```powershell
py -m http.server 5500
```

```powershell
npx serve .
```

或直接執行：

```powershell
.\run-server.bat
```

```powershell
.\run-serve.bat
```

開啟瀏覽器：`http://localhost:5500`

---

## 2. 首頁與頁面切換

1. 進入首頁，確認看到「我的旅行」與兩顆按鈕。
2. 點「開啟新旅程」可進新旅程頁。
3. 點「返回」可回首頁。
4. 點「瀏覽旅程」可進旅程列表頁。

預期結果：頁面切換正常，無白屏或錯誤訊息。

---

## 3. 建立旅程

1. 進入「建立新旅程」。
2. 輸入名稱/國家/地區/出發/回程日期。
3. 點「確定」。

預期結果：
- 進入旅程主頁。
- 標題顯示旅程名稱。
- 日期範圍可自動產生 Day 清單（到行程頁可見）。

---

## 4. 行程編輯

1. 在旅程主頁點「行程」。
2. 點任一 Day。
3. 在「食」輸入早餐/午餐/晚餐。
4. 在「景」新增 1 筆景點，按確認。
5. 在「行」選交通工具並填備註。
6. 在「備註事項」輸入文字。
7. 點「儲存當天行程」。

預期結果：
- 顯示「已儲存當天行程」。
- 返回日期列表。

---

## 5. IndexedDB 持久化

1. 完成至少 1 筆旅程與 1 天行程後，直接重整頁面（F5）。
2. 進入「瀏覽旅程」再打開原旅程。

預期結果：
- 旅程仍存在。
- Day 內容仍可讀到先前資料。

---

## 6. 匯出 JSON

1. 到「瀏覽旅程」頁。
2. 點「匯出 JSON」。

預期結果：
- 下載檔名格式為 `my-travel-backup-YYYY-MM-DD.json`。
- 檔案內容包含 `trips` 陣列。

---

## 7. 匯入 JSON

1. 先匯出一份 JSON。
2. 點「匯入 JSON」並選剛下載的檔案。

預期結果：
- 顯示「匯入成功」。
- 旅程列表刷新並顯示匯入資料。

---

## 8. 基本異常測試

1. 新旅程輸入回程早於出發，按確定。
2. 景點名稱空白直接按確認。
3. 匯入非 JSON 或格式錯誤 JSON。

預期結果：
- 出現對應警示訊息，且資料不被錯誤覆蓋。

---

## 9. PWA 基本檢查

1. 開 DevTools -> Application。
2. 檢查 Manifest 有被載入。
3. 檢查 Service Worker 已註冊。

預期結果：
- Manifest 與 Service Worker 狀態正常。
